# Rust Raid Loadout — レイド装備プランナー（非公式）

Rust のレイド参加者ごとに「手持ち(6)・バッグ(24)・装備(8)」に入れるアイテムを設定し、
レイド当日にそれを見ながら準備できるサイト。設定したアイテムは **工業用コンベアのフィルターJSON** として
コピーでき、ゲーム内の Paste ボタンでそのまま貼り付けられる。

## 起動

```bash
node server.js
```

→ http://localhost:8741 （ポートは `PORT` 環境変数で変更可）。
`index.html` は `items.json` を fetch するので **file:// では動かない**。静的ホスティング（GitHub Pages 等）ならフォルダごと置くだけで動く。

## 使い方

| 操作 | 方法 |
|---|---|
| 参加者を追加 | 左の「＋追加」または「テンプレ」（爆破担当／前衛／ビルダー／補給の4役） |
| アイテムを入れる | 空スロットをクリック → 検索（日本語名・英語名・shortname のいずれでも。例 `C4` `ロケラン` `ammo.rifle`）|
| 数量を変える | 入っているスロットをクリック → 数量入力（Enterで確定）|
| バックパック | 編集エリア下部のプルダウンで「背負わない／小型(12)／大型(28)」。中身もスロットで設定。準備リスト・コンベアには中身＋本体×1が入る |

詳しい使い方・運用方法は **manual.html**（アプリ右上「📖 使い方」）を参照。
| 入れ替え | ドラッグ＆ドロップ（スマホは「移動」ボタン→移動先タップ）|
| 削除 | 右クリック、またはスロットクリック→削除 |
| レイド当日 | 右上「⚔ レイド表示」：大きな表示＋準備チェックリスト |
| 共有 | 「🔗 共有リンク」：計画全体が URL に圧縮して入る（サーバ保存なし）。受け取った人はそのURLを開くだけ |
| コンベア | 右下「工業用コンベア フィルター書出」→「📋 コピー」→ ゲーム内コンベアのフィルター画面で **Paste** → **Apply** |

コンベアは1台につき12件（版によって16件）までなので、超える分は自動で複数台に分割される。
「数量を Max に反映」ON のとき `MaxAmountInOutput` に数量が入り、「その数だけ出力側の箱に移す」動作になる。

データはブラウザの localStorage に保存される（PC/ブラウザごと）。別端末に持っていくときは共有リンクか「書出/読込」を使う。

## ファイル構成

```
index.html          アプリ本体（単一ファイル・外部依存なし）
items.json          アイテム台帳 {s:shortname, id, n:英語名, c:カテゴリ, j:[日本語別名], r:レイド定番フラグ}
icons/64/*.png      アイコン（一覧用）      ← Facepunch 公式CDN由来
icons/256/*.png     アイコン（ツールチップ用）
server.js           開発用静的サーバ
tools/
  fetch_icons.js    公式CDN https://files.facepunch.com/rust/item/{shortname}_{64|256}.png から並列取得（既存はスキップ）
  build_items.js    公式Wiki(Items_Overview) + rusthelp の一覧をマージ → items_raw.json
  finalize_items.js items_raw + カテゴリ補完 + jp_aliases + アイコン有無 → items.json
  jp_aliases.json   日本語名・通称（手書き。ここを増やせば検索が良くなる）
```

## 月次更新（フォースワイプ後に新アイテムが増えたとき）

1. 公式Wiki `https://wiki.facepunch.com/rust/Items_Overview` と `https://rusthelp.com/tools/admin/item-list` の HTML を保存
2. `node tools/build_items.js <wiki.html> <rusthelp.html> tools/items_raw.json tools/shortnames_extra.txt`
3. `node tools/fetch_icons.js tools/items_raw.json`（新規分だけ取得される）
4. `node tools/finalize_items.js tools/items_raw.json <items_text.md> tools/jp_aliases.json items.json`

## 権利表記

- 当サイトは Facepunch Studios Ltd. と提携しておらず、承認・後援も受けていない非公式ファンツール。
- アイテム画像・名称は Facepunch Studios Ltd. の財産。利用は Facepunch の
  [Fan Content and Broadcast Guidelines](https://facepunch.com/legal/ugc)（コミュニティサイト作成を明示的に許可・「公式」を名乗らない・第三者IPは対象外）に従う。
- 画像は公式Wikiが配信している CDN（`files.facepunch.com`、CORS 全開放）から取得し、自前で再ホストしている（ホットリンクしない）。
- 広告等で他社製品の宣伝に使う場合は Facepunch の書面承認が必要（ガイドライン原文）。不明点は legal@facepunch.com へ。
